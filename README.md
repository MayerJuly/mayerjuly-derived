# derived-store

Lightweight, framework-agnostic state manager with a minimal API.  
Works in any JavaScript environment: Node.js, frontend, workers.  
React bindings will be available in a separate entrypoint (`derived-store/react`).


## 📦 Installation

```bash
npm install @mayerjuly/derived-store